package data.medien.validator;

import data.medien.Musik;

public class MusikValidator extends MediumValidator<Musik> {
	@Override
	public boolean validate(Musik object) {
		return super.validate(object);
	}
}
