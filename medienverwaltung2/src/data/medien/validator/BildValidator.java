package data.medien.validator;

import data.medien.Bild;

public class BildValidator extends MediumValidator<Bild> {
	@Override
	public boolean validate(Bild object) {
		return super.validate(object);
	}
}
